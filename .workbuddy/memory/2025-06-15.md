# 2025-06-15

- 修复：红心添加/取消后"我喜欢"页面不刷新
  - 问题根因：FavoritesPage 从酷狗 API 拉取歌曲列表，不消费 FavoritesProvider 本地数据。toggle 后 Provider 通知了，但页面要么不响应，要么去拉远程数据但服务器还没同步完。
  - 修复：`_onFavoritesChanged` 回调中，对"我喜欢"歌单直接从 `FavoritesProvider.favorites` 读取本地数据，不走远程 API。`_loadPlaylistSongs` 也加了同样逻辑（优先本地，本地空时才走 API 兜底）。
  - 修改文件：`lib/modules/user/favorites_page.dart`

- 修复：toggleFavorite 歌单 ID 硬编码问题
  - 问题：`favoriteListId` 硬编码为 `'2'`，应动态获取
  - 修复：改为调用 `_getMyFavoritePlaylist()` 获取真实 `listId`
  - 删除收藏时直接传 `song.id`（hash），服务端支持非数字值自动转为 hash 格式
  - 修改文件：`lib/providers/favorites_provider.dart`

- 重构：FavoritesPage 改为分组折叠式布局 + 批量管理
  - 按创建/收藏分组显示歌单（基于 `listCreateUserid == userid` 判断）
  - 每组可展开/收起（accordion），超过5个时收起状态只显示前5条+展开按钮
  - 长按进入管理模式，支持多选批量删除（自己创建的删除、收藏的取消收藏）
  - 修复收藏别人歌单无内容的问题：收藏的歌单用 `getPlaylistSongs(globalCollectionId)` 加载，自建歌单用 `getPlaylistSongsByListid(listId)` 加载
  - 修改文件：`lib/modules/user/favorites_page.dart`（完整重写）
