# 主页导航优化计划（Tab 精简 → 4 个默认 Tab）

> 目标：解决主页底部导航"冗余、不够简约"的问题。按 MD3E（Material 3 Expressive）原则精简信息层级，按 Karpathy 原则做**外科手术式改动**（只改必要处、可逐项验证、不顺手重构无关代码）。

---

## 一、现状与问题

当前主页默认底部 6 个 Tab（定义见 `kugou_api_server` 无关，Dart 侧在 `lib/providers/tab_config_provider.dart` 的 `kDefaultTabs`）：

```
LaunchPad / 发现 / 本地音乐 / 我收藏 / 私人FM / 我的
```

且另有 10 个可选 Tab（封面流 / 搜索 / 排行榜 / 编辑精选 / 听歌识曲 / 听书 / 场景音乐 / 频道 / 刷刷 / 设置），用户可同时开启，底部导航会无限膨胀。

**冗杂来源**：

1. **6 个默认 Tab 偏多**：主流音乐 App 通常 3~5 个，底部信息密度过高。
2. **LaunchPad 与底部导航功能重叠**：LaunchPad 本质是"导航网格"，把底部已有的 Tab 又平铺了一遍，概念重复。
3. **设置两条入口**："我的"页右上角已有齿轮进入设置，且 settings 又是可选 Tab，入口冗余。
4. **功能重合**："我的"页已有 历史/排行/云盘/下载/已购 快捷入口；本地音乐 / 私人FM / 收藏 彼此独立，占用底部名额。

---

## 二、优化目标（已确认的决策）

| 决策点 | 结论 |
|--------|------|
| 默认 Tab 数量 | **4 个** |
| 默认 4 个 Tab 及顺序 | **发现 → 收藏 → LaunchPad → 我的** |
| 原"本地音乐" | 移出默认，降级为**可选 Tab** |
| 原"私人FM" | 移出默认，降级为**可选 Tab** |
| settings 设置 | 保留**独立可选 Tab**（默认隐藏，不进"我的"） |
| 被移除 Tab 的内容去向 | 通过 **LaunchPad 网格**一键可达 |

**最终的默认导航**：

```
⚠️ 4. 我的    ← 🎨 3. LaunchPad    ← ❤️ 2. 收藏    ← 🧭 1. 发现
（首屏为"发现"）
```

其余功能（本地音乐、私人FM、封面流、搜索、排行榜、编辑精选、听歌识曲、听书、场景音乐、频道、刷刷、设置）全部收进 **LaunchPad** 与设置管理，不占底部。

---

## 三、改动方案（分步骤 + 验证）

核心改动集中在**一个文件**：`lib/providers/tab_config_provider.dart`。页面组件与路由**无需改动**（对 4~16 个 Tab 天然兼容）。

### Step 1：调整默认 Tab 列表

文件：[tab_config_provider.dart](file:///c:/Users/32732/Desktop/TRAE%20SOLO/private_md3music/lib/providers/tab_config_provider.dart)

把 `kDefaultTabs` 改为（保持 `user` 不可隐藏）：

```dart
const List<TabItem> kDefaultTabs = [
  TabItem(id: 'discover', label: '发现'),
  TabItem(id: 'favorites', label: '收藏'),
  TabItem(id: 'launchpad', label: 'LaunchPad'),
  TabItem(id: 'user', label: '我的', isRemovable: false),
];
```

把 `library`、`fm` 从 `kDefaultTabs` 移入 `kOptionalTabs`（保持原有 id/label 不变）：

```dart
const List<TabItem> kOptionalTabs = [
  TabItem(id: 'library', label: '本地音乐'),   // 原默认，现为可选
  TabItem(id: 'fm', label: '私人FM'),           // 原默认，现为可选
  TabItem(id: 'coverflow', label: '封面流'),
  // ...其余不变：search / charts / ip / recognition / audiobook / scene / channel / brush / settings
];
```

- ✅ **验证**：`flutter analyze` 无报错；`kAllAvailableTabs` 仍 = 默认 + 可选，无 id 丢失或重复。

### Step 2：兼容既存用户

`_load()` 会按已持久化的 `order` / `hidden` 重建列表；`launchpad` 在升级时保持默认显示（现有分支已支持）。若老用户持久化的 order 里仍含 `library`/`fm`，会继续显示（尊重用户自定义，**不做强制迁移**）。

- ✅ **验证**：手工跑一次 App，确认首屏为"发现"，底部显示 4 个 Tab。

### Step 3：可选 Tab 默认隐藏语义确认

`_hiddenTabs` 默认取自 `kOptionalTabs`（构造函数第 76~77 行）。新用户走"order 为空"分支时，`library`/`fm` 会自动进入隐藏列表 → 默认只显示 4 个，符合预期。

- ✅ **验证**：清空 App 数据启动，底部恰为 发现/收藏/LaunchPad/我的 4 个。

---

## 四、后续可选的简约化增强（本计划之外，标记为"如需要再做"）

> 已确认"设置保留可选 Tab、本地音乐/私人FM 收进 LaunchPad"。以下为**可选项**，不阻塞本次 4-Tab 精简，避免一次性改动过大。

1. **移除 LaunchPad 网格里与底部重复的 Tab**：现 LaunchPad 会列出除"我的/自身"外的所有 Tab，仍含 发现/收藏。可考虑 LaunchPad 只展示"被隐藏的可选 Tab"，进一步去重。
2. **"我的"页入口聚合**：排行榜（listen_ranking）与"发现"里的排行榜内容重叠，可合并入口。
3. 视觉层面（MD3E）：保持 `labelBehavior = onlyShowSelected`（未选中的 Tab 只显示图标、不显示文字），结合 4 Tab 进一步降低底部拥挤感——此项当前已实现，无需额外改动。

---

## 五、明确"不做"（避免过度工程）

- **不改**任何页面组件、路由逻辑（`app.dart` 的 `_buildPageForTab` / LaunchPad / 发现 / 收藏 / 我的 页面均不动）。
- **不删** LaunchPad、本地音乐、私人FM 的页面与功能，仅调整它们在导航中的展示/隐藏。
- **不新增**抽象、不再造配置系统（沿用现有 `TabConfigProvider` 的显示/隐藏/排序机制）。
- **不做**强制迁移老用户的持久化配置（尊重自定义，代码已兼容）。

---

## 六、验收标准（可逐项验证）

| # | 验收点 | 判定 |
|---|--------|------|
| 1 | 新用户 / 清数据启动 | 底部恰好 **4 个 Tab**：发现、收藏、LaunchPad、我的；首屏为"发现" |
| 2 | 功能可达 | 本地音乐、私人FM、设置等从 **LaunchPad** 与设置管理均可进入，不失效 |
| 3 | 老用户升级 | 自定义 Tab 排序/隐藏不被破坏（尊重持久化配置） |
| 4 | 静态检查 | `flutter analyze` 无新增 error / warning |
| 5 | 无回归 | 收藏、发现、我的、LaunchPad 四个页面行为与改动前一致 |

---

## 七、改动文件清单

- `lib/providers/tab_config_provider.dart` —— 唯一必改文件（默认/可选 Tab 列表调整）

---

## 八、各 Tab 页面 UI 优化评审（覆盖 4 个默认 Tab）

> 补充说明：以下按 **优先级** 分级 ——
> **P0 = 缺陷/无效交互，建议本次一并修复**；
> **P1 = 视觉与动效统一（MD3E 原则），跟随 4-Tab 精简一起做更顺**；
> **P2 = 结构性精简，改动较大需单独确认**。
> 每条均为**独立可验证**的小改动，不互相阻塞。

### 8.1 发现页（现状冗杂点）

| 级别 | 位置 | 问题 | 优化建议 |
|------|------|------|----------|
| **P0** | 主题歌单"查看更多" | `_buildThemeMusicSection` 的 `查看更多` 按钮 `onPressed: () {}` 是**空实现死按钮** | 移除该空按钮，或接上真实跳转 |
| **P0** | 背景视觉 | `_buildBannerSection` 纯装饰性问候 Banner（占 140+ 高度、无功能） | 与顶栏标题 `MD3MusicPro` 信息重复；建议精简为"每日推荐"可点入口，或缩小高度 |
| **P1** | 整页区块 | 5 个区块卡片形态/高度不统一：每日推荐→ActionChip(72)、主题歌单→封面卡(180)、场景音乐→图标卡(110)、热门歌单→AlbumCard(200)、排行榜→AlbumCard(200) | 收敛为 2 种统一卡片范式（横滑封面卡 / 图标入口卡），对齐 MD3E"容器分组一致性" |
| **P1** | 折叠交互 | 用 `expand_more` + 200ms `AnimatedCrossFade`，与收藏页 `chevron_right` + 300ms `SizeTransition` 不一致 | 统一折叠图标与动效时长 |
| **P2** | 场景音乐 | 不可点击、无"查看更多"，图标卡信息量低 | 可整合进主题歌单/热门歌单，减少横向分区数量 |

### 8.2 收藏页（现状冗杂点）

| 级别 | 位置 | 问题 | 优化建议 |
|------|------|------|----------|
| **P1** | 折叠动效 | `_GroupSection` 展开图标 `chevron_right` turns 0.25，与发现页不一致 | 随 8.1 一并统一折叠样式 |
| **P2** | 列表密度 | 歌单/专辑/歌手 每行重复"封面 + 名称 + N首 + chevron"，信息密度偏高 | 保留结构；如需简约可合并为网格视图（本改为大改，需单独确认） |
| — | 调试残留 | `_buildAlbumTile` 等处有较多 `debugPrint` | 非 UI 问题，仅提示：可顺带清理调试日志（本次不动） |

### 8.3 LaunchPad（配合 4-Tab 精简）

| 级别 | 位置 | 问题 | 优化建议 |
|------|------|------|----------|
| **P1** | 网格内容 | 会列出**所有** Tab（含底部已显示的 发现/收藏），与底部导航重复 | 改为**只展示"未在底部显示的隐藏 Tab"**，已显示的不再重复；同第四节第 1 条 |

### 8.4 我的页（现状冗杂点）

| 级别 | 位置 | 问题 | 优化建议 |
|------|------|------|----------|
| **P0** | 日历左右切换 | 签到日历的 `‹`/`›` 按钮 `onPressed: () {}` 是**空实现死按钮** | 移除或实现月份切换 |
| **P1** | 信息密度 | 单屏堆叠：头像卡(含等级胶囊) + 5 格操作网格 + VIP卡 + 打卡日历 + 3 个领取按钮(签到/听歌领取/广告领取) | 折叠式收纳：领取/打卡类强度营销的功能收进二级页或折叠区，首屏只留 头像+操作网格 |
| **P2** | 操作网格重复 | 历史/排行/云盘/下载/已购 与 LaunchPad 概念重复 | 与 LaunchPad 统一收口（本次不动，列为后续项） |

### 8.5 统一建议（跨 Tab）

1. **折叠图标/动效对齐**（发现 ↔ 收藏）：统一为一种折叠视觉 + 一种动效，避免两种实现并存。
2. **P0 死按钮清零**：主题歌单"查看更多"、日历左右箭头 —— 本次一并修复。
3. `labelBehavior = onlyShowSelected` 已生效，4-Tab 下底部自然更空旷（无需额外改）。

---

## 九、建议落地顺序（供评审）

| 批次 | 内容 | 理由 |
|------|------|------|
| 批次一（核心） | 第三节 Step 1~3：默认 4-Tab 精简（唯一改 `tab_config_provider.dart`） | 只改一个文件、零页面改动，先落地 |
| 批次二（P0 修复） | 8.1/8.4 的死按钮（主题歌单"查看更多"、日历箭头） | 消灭无效交互，改动小 |
| 批次三（P1 统一） | 8.1~8.4 视觉/折叠动效统一、LaunchPad 只列隐藏 Tab、发现页区块收敛 | 需要分页面小改，可逐条验证 |
| 批次四（P2，待确认） | 我的页信息密度收纳 / 收藏网格化 / 场景音乐整合 | 改动较大，单独评审后再做 |

> 每个批次各自独立可验证，可择期、独立回滚。

---

*计划文档仅供评审，确认后再进入实现。*