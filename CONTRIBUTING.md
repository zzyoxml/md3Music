# 贡献指南（Contributing）

感谢你愿意帮助改进 MD3Music！为了让问题能被高效定位和修复，请先阅读本指南。

## 交流渠道

- **使用疑问 / 讨论**：请在 [Discussions](https://github.com/zzyoxml/md3Music/discussions) 发帖，不要用 Issue 提问。
- **Bug / 缺陷报告**：请使用 [Issue 模板](https://github.com/zzyoxml/md3Music/issues) 提交，模板会自动引导你填写必要信息。

## 提交 Bug 报告前

1. 先搜索现有 Issue，确认不是重复报告。
2. 确认你使用的是**最新版本**（在“关于”页查看版本号）。
3. 确认你已尽力排除环境因素（网络、设备、权限）。

## 必须提供的信息

为了让崩溃与音频问题可以被复现和定位，以下信息**缺一不可**：

- **崩溃问题**：必须提供 `adb logcat` 抓取的关键日志（崩溃通常发生在日志 30–60 秒内）。推荐在“设置 → 关于 → 导出日志”生成一份包含设备信息的日志文件后上传。
- **音频相关问题**：必须说明触发问题音频的来源（酷狗概念版 / 网易云音乐 / 其他平台 / 本地原有文件），并说明该音频是否经过**非标准标签修改**（异常 ID3/版权/歌词头处理等）。仅是“我的文件听不了/闪退”而没有任何来源与日志描述的报告，我们无法定位。
- **环境信息**：应用版本、设备型号、Android 版本、是否 Root。

## 处理规则

以下 Issue 将被**直接关闭并锁定**：

- 拒绝提供必要信息（日志、音频来源、标签修改情况）后仍反复催办；
- 对维护者或其他用户进行人身攻击 / 辱骂；
- 恶意刷 issue、无关灌水。

## 提交代码 / PR

- 先说明改动目标，保持 `pr` 小而聚焦，避免夹带无关重构。
- 遵循仓库现有代码风格，不引入未请求的特性。
- 涉及音频标签解析的改动，请在描述中注明抗异常/兜底策略。

## 附：如何抓取日志

```bash
# 连接开启 USB 调试的设备后：
adb logcat -c          # 清空
# 手动触发一次崩溃
adb logcat *:E -d      # 抓取错误日志
```

若没有安装 adb，可使用应用内“导出日志”功能。