{
  "name": "md3Music",
  "version": "1.0.0",
  "description": "本项目已成功修复以下问题：",
  "main": "index.js",
  "directories": {
    "lib": "lib",
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:api": "node kugou_api_server/app.js",
    "start:flutter": "flutter run -d edge",
    "start:all": "concurrently \"npm run start:api\" \"npm run start:flutter\"",
    "install:deps": "npm install concurrently --save-dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@playwright/test": "^1.60.0"
  },
  "devDependencies": {
    "concurrently": "^10.0.0"
  }
}
